# E2B Sandbox Agent Requirements

# Core dependencies
e2b-code-interpreter>=0.0.11b20
python-dotenv>=1.0.0

# Note: These are installed in the sandbox during runtime
# anthropic>=0.50.0  (installed in sandbox)
# aiohttp>=3.9.0     (installed in sandbox)

# Auth & database
authlib>=1.3.0
python-jose[cryptography]>=3.3.0
aiosqlite>=0.19.0
itsdangerous>=2.1.0
httpx>=0.24.0
starlette>=0.27.0

# For local development/testing
claude-agent-sdk #  (if using SDK locally)
